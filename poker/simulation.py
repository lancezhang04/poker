from poker import Game, Card
from typing import List
from tqdm import tqdm


def run_monte_carlo(hole_cards: List[Card], community_cards: List[Card]=None,
                    num_players: int=6, num_iters: int=10000):
    pass
